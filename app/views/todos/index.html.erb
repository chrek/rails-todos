<% @todos.each do |todo| %>
  <div class="index_row_clearfix">
    <h2 class="todo_title"><%= link_to todo.title, todo %></h2>
    <p class="todo_sub_title"><%= todo.description %></p>
  </div>
<% end %>
<div class="links">
  <%= link_to 'New Todo', new_todo_path %></td>
</div>
<!--- COMMENTED-OUT CODE 
<% if false %>
<p id="notice"><%= notice %></p>

<h1>Todos</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @todos.each do |todo| %>
      <tr>
        <td><%= todo.title %></td>
        <td><%= todo.description %></td>
        <td><%= link_to 'Show', todo %></td>
        <td><%= link_to 'Edit', edit_todo_path(todo) %></td>
        <td><%= link_to 'Destroy', todo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Todo', new_todo_path %> 
<% end %>
-- END OF COMMENTED-OUT CODE -->